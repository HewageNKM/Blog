import React from 'react';
import {SafeAreaView, ScrollView, Text} from "react-native";

const SignUp = () => {
    return (
        <SafeAreaView>
            <ScrollView>
                <Text>SignUp</Text>
            </ScrollView>
        </SafeAreaView>
    );
};

export default SignUp;