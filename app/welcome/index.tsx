import React from 'react';
import {SafeAreaView, ScrollView, Text} from "react-native";

const Index = () => {
    return (
        <SafeAreaView>
            <ScrollView>
                <Text>Welcome</Text>
            </ScrollView>
        </SafeAreaView>
    );
};

export default Index;