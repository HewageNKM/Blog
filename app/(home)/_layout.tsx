import React from 'react';
import {Tabs} from "expo-router";

const _Layout = () => {
    return (
        <Tabs>
            <Tabs.Screen name="home"/>
        </Tabs>
    );
};

export default _Layout;